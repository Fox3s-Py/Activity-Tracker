# Версии зафиксированы намеренно — см. комментарий в requirements.txt.
# Отдельно от requirements.txt: эти пакеты нужны только для разработки
# и CI, ставить их в проде незачем (issue #19).

pytest==9.1.1
httpx==0.28.1
pytest-cov==7.1.0